services:
  ch-ai:
    image: ${AI_IMAGE:-ch-ai:local}
    container_name: myapp-ch-ai
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "${AI_HOST_PORT:-8000}:8000"
    volumes:
      - ../cache:/app/cache
